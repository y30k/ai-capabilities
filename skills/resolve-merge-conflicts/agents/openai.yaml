interface:
  display_name: "Resolve Merge Conflicts"
  short_description: "Resolve Git merge and rebase conflicts"
  default_prompt: "Use $resolve-merge-conflicts to resolve these Git conflicts while preserving the intent of both sides and validating the result."
