interface:
  display_name: "Address PR Review Comments"
  short_description: "Address and resolve unresolved PR feedback"
  default_prompt: "Use $address-pr-review-comments to process only unresolved PR threads, validate fixes, reply with what changed and why, push, and resolve fully addressed threads."
