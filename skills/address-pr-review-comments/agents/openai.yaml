interface:
  display_name: "Address PR Review Comments"
  short_description: "Fix PR threads with SHA replies"
  default_prompt: "Use $address-pr-review-comments to resolve review threads, validate fixes, reply with commit short SHAs when possible, then push the update."
