interface:
  display_name: "Implement PRD Stories"
  short_description: "Implement, stage, and advance PRD stories"
  default_prompt: "Use $implement-prd-stories to implement the next unblocked story only in the current repo, validate and stage it, mark it Done, and move newly unblocked dependent stories to Ready across repositories."
